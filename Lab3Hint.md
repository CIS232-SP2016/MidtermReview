Create the encoded String using StringBuilder and looping through each character of the original String.
Use writeUTF to write the String to the binary file.

Use the readUTF to access the String from the binary file.
Create the decoded String using StringBuilder and looping through each character of the encoded String.
